{
  "name": "irticalen-api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260921.1",
    "typescript": "^6.0.3",
    "vitest": "^5.0.1",
    "wrangler": "^4.135.0"
  }
}
